{
  "mcpServers": {
    "github": {
      "command": "uvx",
      "args": ["--from", "github-mcp-server", "github-mcp-cli"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
